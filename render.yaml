services:
  - type: web
    name: Bot ig
    env: python
    plan: free

    buildCommand: |
      python3.10 -m pip install --upgrade pip
      python3.10 -m pip install -r requirements.txt

    startCommand: python3.10 -m gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 8
